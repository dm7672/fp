using TagsCloud.ConsoleClient;
namespace TagsCloud
{
    internal static class Program
    {
        private static int Main(string[] args)
        {
            return new ConsoleApplication().Run(args);
        }
    }
}
